#ifndef THROTTLE_HPP
#define THROTTLE_HPP

/*


#include "Pedals.hpp"

class Throttle : public interface_Pedals {


    public:

        Throttle(MyAtmega32u4 &controller);
        void begin() override;
        uint8_t read() override;
        double getValue() const override;
        void uploadSettings() override;
        void downloadSettings() override;

    private:

        pod_pedals data_;
        MyAtmega32u4 *controller_;
};


*/

#endif // THROTTLE_HPP
