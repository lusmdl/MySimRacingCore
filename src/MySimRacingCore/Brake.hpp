#ifndef BRAKE_HPP
#define BRAKE_HPP

/*

#include "Pedals.hpp"

class Brake : public interface_Pedals {


    public:
        Brake(MyAtmega32u4 &controller);
        void begin() override;
        uint8_t read() override;
        double getValue() const override;
        void uploadSettings() override;
        void downloadSettings() override;

    private:

        pod_pedals data_;
        MyAtmega32u4 *controller_;
};



*/
#endif // BRAKE_HPP
